# Power BI local / transient state (never commit)
**/.pbi/
**/.pbi/localSettings.json
**/.pbi/cache.abf

# Large source data — download from the MoMA open-data repo instead (see README)
Artworks.csv
Artists.csv
MoMA_OnView.xlsx

# Exported binary (share via GitHub Releases, not the repo tree)
*.pbix

# Local scratch / working files (underscore-prefixed) — clean copies live in screenshots/ and build_report.py
_*.py
_shot_*.png
.claude/
